apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  username: {{ .Values.keycloak.username.value | b64enc }}
  password: {{ .Values.keycloak.password.value | b64enc }}
